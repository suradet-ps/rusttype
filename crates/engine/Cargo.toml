[package]
name = "engine"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Typing state machine, WPM/accuracy calc, keystroke event log"

[dependencies]

[dev-dependencies]
